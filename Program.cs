using university.Controllers;

namespace university
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // StudenteController.getIstanza().stampaStudenti();
            // StudenteController.getIstanza().inserisciStudente("VINCVG", "Vincent", "Vega");
            // StudenteController.getIstanza().cercaStudente(2);

            // CorsoController.getIstanza().stampaCorsi();
            // CorsoController.getIstanza().inserisciCorso("OOP", "OOPJAVA", 175);
            // CorsoController.getIstanza().cercaCorso(3);


            // IscrizioneController.getIstanza().stampaIscrizioni();
            // IscrizioneController.getIstanza().inserisciIscrizione(3, 3);
            // IscrizioneController.getIstanza().cercaIscrizionePerCorso(1);
            // IscrizioneController.getIstanza().cercaIscrizionePerStudente(2);
        }
    }
}